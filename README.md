# MyWeatherApp

## Build release JAR distribution

From the repository root, run:

```bash
cd MyWeatherApp
mvn clean package
```

Release artifacts are generated in:

- `target/myweatherapp-1.0.0.jar`
- `target/myweatherapp-1.0.0-jar-with-dependencies.jar`
